// Optional entry point: a thin chart renderer wrapper.
// Requires `react-klinecharts` as an OPTIONAL peer dependency — install it
// explicitly when importing from `react-klinecharts-ui/chart`.
export { ChartCanvas, type ChartCanvasProps } from "./ChartCanvas";
